import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "UniConnect - AI-Powered University Student Platform",
  description:
    "Connect, learn, and grow with fellow students through our comprehensive platform featuring community hub, teammate finder, study materials, AI chatbot, learning dashboard, and residence finder.",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
